# UI改善指示：プロ品質のインタラクション＆見栄え強化

## 背景
このアプリ（Theater Method Lab ワークショップメーカー）は営業ツールとしてクライアントにURLを送る想定。
競合ツール（SessionLab, Butter.us等）はドラッグ＆ドロップ、スムーズなトランジション、ホバーエフェクトなど洗練されたマイクロインタラクションを実装しており、初見で「プロが作ったツール」という印象を与えている。
現在のアプリは機能面では十分だが、インタラクションの層が薄いため「素人っぽさ」が出てしまっている。
既存のダークテーマ・色設計は維持しつつ、プロ品質のマイクロインタラクションを追加する。

---

## 修正1：CTAボタンのヒエラルキーとフィードバック

「カリキュラム作成」ボタンがページ内で最も重要なアクション。

やること：
- 「カリキュラム作成」ボタン（プライマリ）を最も目立たせる（塗りつぶし・十分なサイズ・色の強調）
- その他のボタン（再生成・詳細表示など）をセカンダリとして差別化する（枠線のみ or 薄い背景にするなど）
- ホバー時：background-colorの変化 + transform: translateY(-2px) + box-shadowの追加（浮き上がり感）。transition: all 0.2s ease-out で滑らかに
- クリック時（:active）：transform: scale(0.98) でタクタイルな押し込み感
- ボタンのpadding/font-sizeがスマホでも押しやすいサイズ（最低44px高さ）を確保する

## 修正2：選択チップ（目的・レベル・時間）のインタラクション

やること：
- 選択中のチップ：背景色のアクセント表示に加え、transition: all 0.2s ease で切り替わりを滑らかにする
- ホバー時：未選択のチップにも軽い背景色の変化をつけて「選択できる」ことを示す
- 選択切り替え時のtransition（色・ボーダーの変化）が瞬間的ではなくスムーズに遷移するようにする

## 修正3：カリキュラム結果カードの視認性＋登場アニメーション

ファシリテーターは生成結果を「スクリーンショットで共有」「印刷して持っていく」ことがある。

やること：
- カード背景色を周囲よりやや明るくして、カードが浮いて見えるようにする（例：rgba(255,255,255,0.05)〜0.1程度の白を乗せる。現状確認の上で適切な値にすること）
- カード内テキストのコントラスト比をWCAG AA基準（4.5:1以上）で確保する
- エクササイズ名（タイトル）と説明文でfont-size/font-weightの差を明確にする
- カード間の余白（gap/margin）を十分にとる
- **カードの登場アニメーション**: カリキュラム生成時、カードが上から順番にフェードイン＋スライドアップで登場する。各カードに50〜100msずつディレイをつけてスタッガードアニメーションにする（CSS: opacity 0→1, transform: translateY(20px)→0, transition: 0.3s ease-out）
- カードのホバー時：translateY(-4px) + box-shadow強化で浮き上がり効果をつける（transition: 0.2s ease-out）

## 修正4：スマホ表示の確認・調整

やること：
- 結果カードがスマホ幅（375px〜）で崩れていないか確認し、必要なら修正
- 選択チップがスマホで折り返したとき、間隔が詰まりすぎていないか確認
- タップターゲット（ボタン類）が最低44px × 44pxを確保しているか確認
- ホバーエフェクトがスマホでは無効になるようにする（@media (hover: hover) を使うこと。タッチデバイスでは不要）
- 問題があれば修正。なければそのまま

## 修正5：全体的なトランジション追加

やること：
- ページ内のインタラクティブ要素（ボタン、チップ、カード、リンクなど）すべてに transition を設定する。何もついていない要素があれば追加する
- transitionの基本値：0.15s〜0.25s ease-out（速すぎず遅すぎない）
- スライダー（参加人数）のthumbにもホバー時の変化をつける（サイズ拡大 or 色変化）

---

## やらないこと（明示的に除外）
- テーマ色の大幅変更（既存のダークテーマを維持）
- フォントの変更
- 新機能の追加
- パララックスやスクロールトリガーアニメーション（過度な演出は不要）
- prefers-reduced-motion を無視した重いアニメーション。アクセシビリティ対応として以下を必ず追加すること：

```css
@media (prefers-reduced-motion: reduce) {
  * {
    animation-duration: 0ms !important;
    transition-duration: 0ms !important;
  }
}
```

## 完了後
- 変更したファイル名と変更箇所をコミットメッセージに明記すること
- git push まで実行すること
